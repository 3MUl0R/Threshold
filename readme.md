
## Use Openai Codex for reviews on planning and staged code.  Loop staged code reviews until all are resolved
### Codex CLI Usage

```bash
# Start a new review session
codex exec --full-auto "your prompt"

# Continue/resume an existing session
codex exec resume <session-id> --full-auto "follow-up prompt here"
```



### use playwright-cli for end to end testing in the browser
playwright-cli --help


